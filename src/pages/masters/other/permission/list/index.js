import Grid from '@mui/material/Grid'

const Permission = () => {
  return <Grid container>Permission</Grid>
}

Permission.acl = {
  action: 'read',
  subject: 'component'
}

export default Permission
