import Grid from '@mui/material/Grid'

const LineOfBusiness = () => {
  return <Grid container>Line Of Business</Grid>
}

LineOfBusiness.acl = {
  action: 'read',
  subject: 'component'
}

export default LineOfBusiness
